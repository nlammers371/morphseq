"""
neighborhoods.py
----------------
Neighborhood structure utilities for coherence computation.

Intended role: functions that define or query which (embryo, time) pairs
are neighbors for the purpose of coherence gating — e.g., spatially-gated
or topology-aware neighborhood masks.

Currently unused. Home for future neighborhood logic.
"""
