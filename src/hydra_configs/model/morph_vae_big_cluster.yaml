config_target: src.models.legacy_model_configs.morphVAEConfig

ddconfig:
  input_dim: [1, 288, 128]
  n_out_channels: 16 #16
  latent_dim: 100
  n_conv_layers: 5
  frac_nuisance_latents: 0.2

lossconfig:
  kld_weight: 10.0
  metric_weight: 10
  pips_flag: True
  pips_weight: 0.8
  pips_net: "squeeze"
  schedule_metric: True
  schedule_pips: True
  schedule_kld: True
  bio_only_kld: False
  margin: 1.0

dataconfig:
  target: "BasicDataset"
  num_workers: 8
  batch_size: 256
  wrap: True
  root: "/net/trapnell/vol1/home/nlammers/projects/data/morphseq/training_data/20241107_ds/"

trainconfig:
  benchmark: True
  accumulate_grad_batches: 2
  max_epochs: 125