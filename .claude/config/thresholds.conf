# Context monitoring thresholds for intelligent delegation suggestions

# Line threshold - trigger delegation suggestions when conversation exceeds this many lines
LINE_THRESHOLD=100

# Word threshold - trigger delegation suggestions when conversation exceeds this many words  
WORD_THRESHOLD=5000

# Complexity threshold - trigger delegation suggestions based on technical complexity score
COMPLEXITY_THRESHOLD=50

# Sub-agent concurrency limits
MAX_GEMINI_PRO_CONCURRENT=4
MAX_GEMINI_FLASH_CONCURRENT=8
MAX_TOTAL_CONCURRENT=10

# Timeout settings (seconds)
GEMINI_FLASH_TIMEOUT=30
GEMINI_PRO_TIMEOUT=60
PARALLEL_COORDINATOR_TIMEOUT=300