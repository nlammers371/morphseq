# Task 1.2 — FeatureDataset Build + Validate

## Goal
Verify Zarr/Parquet/manifest contract and QC flags are created correctly.

## Checks
- Shapes: `X (N,H,W,C)`, `y (N,)`, `mask_ref (H,W)`.
- Manifest contains channel schema, split policy, QC policy.
- QC outlier flags generated deterministically.

## Pseudo-logic
1. Build tiny synthetic dataset.
2. Write to on-disk contract.
3. Re-load and run validator.
4. Compare in-memory and round-trip summaries.

## Outputs
- `manifest.json`
- `metadata.parquet`
- QC summary table
