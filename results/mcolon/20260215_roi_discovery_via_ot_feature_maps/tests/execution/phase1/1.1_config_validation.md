# Task 1.1 — Config Construction + Validation

## Goal
Confirm config dataclasses/enums produce deterministic, valid training/sweep settings.

## Checks
- Required fields exist and are immutable where expected.
- Lambda/mu resolver returns reproducible values.
- Invalid enum/member combinations fail fast with clear errors.

## Pseudo-logic
1. Build default config.
2. Build custom config override.
3. Validate schema and derived fields.
4. Assert deterministic serialization/hash.

## Outputs
- Config snapshot JSON
- Validation pass/fail log
