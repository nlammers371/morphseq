# Task 1.7 — Lambda/Mu Sweep + Deterministic Selection

## Goal
Ensure sweep table completeness and reproducible parameter selection.

## Checks
- Every grid pair has a result row.
- Selection rule (Pareto knee or epsilon-best) is deterministic.
- Selected ROI remains non-trivial and inspectable.

## Pseudo-logic
1. Run coarse lambda/mu sweep.
2. Persist per-run metrics and complexity stats.
3. Apply deterministic selector.
4. Re-run with same seed and compare selected pair.

## Outputs
- Sweep table (parquet/csv)
- Selection metadata JSON
